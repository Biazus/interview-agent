Arquivo usado apenas para organização de pensamentos (nao deveria estar no repo mas vou deixar por enquanto)

- Uma busca deterministica enquanto o nro de documentos é baixo faz muito sentido
- Embora eu possa usar busca semantica e filtrar por tópico para não trazer fatos irrelevantes

E para uma primeira versão, faz sentido eu gerar duas ou tres respostas prontas para cada pergunta

- EmbeddingProvider.embed (sentence-transformers) e VectorStore.search (qdrant-client, síncrono) são ambos chamadas bloqueantes.
  Não sei se preciso resolver isso mais tarde ou nao seria um bloqueio tao grande em high traffic, preciso analisar.

- Onde pego documentos para serem importados? Seria interessante baixar alguns capitulos de livros de um topico especifico como 
  Kafka para testar e depois faço o resto.

- para importar:
    Pra segunda versão um data pipeline poderia ser usado caso eu queira fazer algo muuuito robusto (over engineering por agora, mass TODO)
    pypdfpara extração simples de texto, ou lib unstructured, que lida com PDF, HTML, DOCX e já devolve texto limpo

- A ideia é usar A2A em breve

- Tracing de chamadas de LLM, contagem de tokens, compilação de custos por sessao.
- OpenTelemetry / OpenInference
- LOGS!!!!!11!

- Ollama não era uma opção até 1h atrás quando vi que o rate limit do openroute é baixissimo...
    Porém gostei do Groq e está servindo bem inicialmente, sem usar com exageros.
    Pensando alto, não precisaria ser um modelo tão robusto pra rodar localmente como fallback, portanto preciso avaliar questao de 
    custo de recursos computacionais (além do que os models :free do OpenRoute ficam constantemente saindo da lista free então não
    sei de alguma solução para cobrir isso - não quero fazer webscrapping de algo que nao pertence ao dominio desse projeto...)
    - Lembrar de não gastar token a toa para nao estourar limit até eu fazer algo em relação a isso (ser pobre é osso)

(DONE) - Tenho que descobrir porque raios os testes tao demorando tanto. Talvez eu esteja inicializando errado alguma coisa que nao deveria.
    First: lineprofiler ou cprofile
(DONE)  Cold start do sentence-transformers
(DONE)  Ps.: criar singletons/cache para EmbeddingProvider e QdrantRetriever
(DONE)  get_domain() recria tudo em cada chamada,
        Colocar modelo menor na avaliação?

(DONE) Preciso parar de fazer push na main (amanhã paro kkk)
(DONE) - Criar protected branch pra eu não ficar postergando isso
(DONE) - Usar issues do Github pra eu nao esquecer as coisas

(DONE)- Github Actions pra ao menos rodar os testes e passar black e isort seria bom tb...
(DONE) Ai já faço um alias local que ao commitar ja passa a bateria de linting e test pra nao colocar sujeira no repo.

- Só de pensar que também falta a parte do frontend, rotas API, db, migrations, auth, rate limit, terraform, aws...
    porém não vou escapar de ter que implementar filas pro protocolo A2A, a menos que implemente com HTTP, o que seria feio.

Meta: 4 commits por dia pra eu não postergar isso tudo.