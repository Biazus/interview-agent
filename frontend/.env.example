# Dev local: leave empty — Vite proxy forwards /auth, /domains, /topics, /interviews, /health to :8000
# VITE_API_BASE_URL=

# Production (Vercel): set to the public API URL (e.g. Render web service)
# VITE_API_BASE_URL=https://interview-agent-api-xxx.onrender.com
